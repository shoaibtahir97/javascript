/*
A function can also be defined using an expression
A function expression can be strored in a variable
These functions are anonymous, they dont have any name
If you want to call the function you have to use the variable name
The function ends with a semi colon because it is part of executable statement
*/ 

var sum = function(a, b) {return a + b;};

sum(5, 5);

console.log(c);
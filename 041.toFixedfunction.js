/*
In arithmetic operation you may favce numbers with many decimal places.
To limit decimal places to specified number you can call toFixed() 
function on number and round last digit.
*/ 

var average = (45 + 76 + 12) / 3; //25.6666666666

var avg = average.toFixed(3); //255.667


var nationality = "Pakistani"; //string

var age = 25; //number

var weight = 72.4; //number

var isFeePaid = true; //boolean



/*
Used to store data/information, its value can be changed.
*/

age = 23; 
       
/*
The variable age is updated. We will not put var keyword as the variable is previously assigned.  
If we put var then it will be new variable 
*/

weight = null;

//In null value assigned to variable as a 'no value'. It takes allocation in memory. null is a object


nationality;
nationality = undefined;

//In undefined value is assigned to the variable but variable is declared. It is a type itself. javascript sets a 
//variable undefined by default, but null can be only assigned by user.
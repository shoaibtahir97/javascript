/*
PARAMETERS:-
    Function parameters are listed inside the parenthesis() in 
the function declaration.


ARGUMENTS:-
    Function arguments are the values received by the function
when it is invoked.
*/ 

function showMessage(message) { //() is Parameter 
    console.log(message);
}


showMessage("Hello World"); // () is Argument